<h1>Our Doctors</h1>
<hr>

<% @doctors.each() do |doctor| %>
    <div> <img src=" <%= doctor.avatar %> " alt=""> </div>
    <h3> <%= "#{doctor.first_name} #{doctor.last_name}" %> </h3>
    <%= link_to "info", doctor_path(doctor.id) %>
    <hr>
<% end %>